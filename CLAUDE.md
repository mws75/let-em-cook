@.claude/skills/let-em-cook/SKILL.md

The import above loads the full project context (architecture, ltc_* data model, auth, OpenAI pipeline, Stripe, docs organization, conventions) at the start of every session — no need to re-explore the codebase for orientation.
